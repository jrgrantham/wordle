// import { createSlice } from "@reduxjs/toolkit";
// // import { createSelector } from "reselect";
// import words from "an-array-of-english-words";

// const slice = createSlice({
//   name: "words",
//   initialState: words,
//   // reducers: {
//   //   userUpdated: (words, action) => {
//   //     words.loading = false;
//   //     const user = action.payload.data;
//   //     const { userId } = user;
//   //     const index = words.data.findIndex((user) => user.userId === userId);
//   //     words.data.splice(index, 1, user);
//   //   },
//   // },
// });

// export default slice.reducer;

// // export const { userUpdated } = slice.actions;

// // export const getUsersById = createSelector(
// //   (state) => state.entities.words.data,
// //   (words) => {
// //     const result = {};
// //     words.forEach(({ name, userId }) => {
// //       result[userId] = name;
// //     });
// //     return result;
// //   }
// // );
